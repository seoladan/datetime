services:
  php:
    build:
      dockerfile: docker/Dockerfile
      target: dev
    user: ${UID:-1000}:${GID:-1000}
    ports:
      - "9003:9003"
    volumes:
      - ./:/var/www/html/
      - ~/.gitconfig:/etc/gitconfig:ro
